package jus.aoo.tri.sort;

import java.util.Comparator;

import jus.aoo.tri.Tri;

public class HeapSort extends Tri {
	
	private int G(int r) {
		return 2*r;
	}
	
	public<T> void constHeap(T[] t, int r , int l, Comparator<T> c) {
		if(G(r) >l)return; // racine seul
		if(G(r) == l) { // Gauche seulement
			constheap(t, )
	}
		
	}

	


}